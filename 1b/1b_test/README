these are tests extracted from codeforces.com, contest 1, problem b.

Each test.N file contains input according to the input specification
of the problem. Each answer.N file contains the expected output for
running the program with test.N as its input.
